[project]
name = "signal-scope"
version = "0.1.0"
description = "Signal Scope"
requires-python = ">=3.12,<3.14"
dependencies = [
    "flask==3.0.3",
    "flask-cors",
]

[tool.vercel]
entrypoint = "src.backend.app:app"
