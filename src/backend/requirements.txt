# SignalScope — Backend Python Dependencies
# ------------------------------------------
# Install with:
#   pip install -r src/backend/requirements.txt
#
# Pin exact versions for reproducibility.

# Core framework
Flask==3.0.3
flask-cors==4.0.1

# Testing
pytest==8.2.2
pytest-flask==1.3.0

# Future: ML inference dependencies will be added here in the model-training milestone
# tensorflow==2.x.x   (added in future session)
# Pillow==x.x.x       (added in future session)
