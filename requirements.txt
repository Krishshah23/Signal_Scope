# SignalScope — Top-Level Python Dependencies
# --------------------------------------------
# This file covers the backend API dependencies.
# For a full install from the project root:
#   pip install -r requirements.txt
#
# Alternatively, install from the backend-specific file:
#   pip install -r src/backend/requirements.txt
#
# ML dependencies (TensorFlow, Pillow, etc.) will be added
# in the model training milestone.

# --------------------------------------------------------------
# Backend — Flask API
# --------------------------------------------------------------
Flask==3.0.3
flask-cors==4.0.1

# --------------------------------------------------------------
# Testing
# --------------------------------------------------------------
pytest==8.2.2
pytest-flask==1.3.0

# --------------------------------------------------------------
# ML — model training and inference (Session 2)
# Requires Python 3.12 (TF 2.18 not yet available for Python 3.14)
# Install in the project venv: .venv/Scripts/python -m pip install -r requirements.txt
# --------------------------------------------------------------
tensorflow==2.18.0
Pillow==10.4.0
numpy==1.26.4
